# Pulse Decision Log

## v1 scope decisions (locked)
- Agency-owned system-of-record only (Option 1).
- No task creation inside Pulse in v1.
- Clients can view synced tasks + compliance metrics (overall + per-VA list).
- No OS idle detection; only check-ins.
- UI must be responsive and support light/dark.
- UI implementation must follow `docs/ui/*` as source of truth.

## v1.1+ (not in v1)
- Client-owned integrations (Model B / BYO tools).
- Client access to full check-in transcript.
- Advanced AI planning/auto-assign.

## Notes
Update this log only when product decisions change.
